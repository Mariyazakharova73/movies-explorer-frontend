import React from "react";
import FormTemplate from "../FormTemplate/FormTemplate";
import { textLogin } from "../../utils/variables";

const Login = () => {
  return <FormTemplate text={textLogin} />;
};

export default Login;
